"""EXP / 레벨 시스템 상수 — 이 파일만 수정해서 전체 튜닝.

CLAUDE.md §4 하드룰:
  - ₩ 액수 비례 금지 (수익률·이벤트 기반만).
  - 손실 감점 없음 (exp ≥ 0 만 입력).
  - 스타일 중립 (3소스 = 배당형·성장형·장기보유형 모두 커버).

설계 근거: docs/level_exp_design.md
"""

# ── ① 배당 EXP ───────────────────────────────────────────────────────────────
DIV_BASE: int = 100          # 배당 이벤트당 고정 EXP (금액 완전 무관)

# ── ② 익절 EXP (수익률 % 기반, ₩ 무관) ───────────────────────────────────────
SELL_W: float = 10.0         # 수익률 1%당 EXP 계수
SELL_CAP_PCT: float = 30.0   # 수익률 상한 (30% 초과도 300으로 고정 → 대박 한 방 지배 방지)
SELL_MIN_PCT: float = 1.0    # 최소 수익률 미만 → 0 EXP (스캘핑 파밍 차단)

# ③ 행동·연속성 EXP = v2. 상수 자리만 예약.
# STREAK_BASE = 10
# HOLD_PER_TICKER = 20

# ── 레벨 곡선 ─────────────────────────────────────────────────────────────────
# need(L) = round(LV_BASE × L^LV_POW)
# Lv1→2: 100 EXP,  Lv10→11: 3,162,  Lv50→51: 35,355,  Lv99→(cap): 98,505
LV_BASE: float = 100.0
LV_POW: float = 1.5

# 만렙 (EXP 원장은 무한 저장 — v2 프레스티지 여지)
LV_MAX: int = 99
