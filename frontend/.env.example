# 프론트엔드 환경변수 (Vite 빌드 시 번들에 삽입됨)
# Railway → Variables 탭에 아래 키를 입력. 로컬 개발은 비워둠.

# 백엔드 Railway URL. 비워두면 vite proxy(개발 서버)를 경유함.
# 배포 순서: 백엔드 먼저 배포 → URL 확인 → 여기에 입력 → 프론트 빌드/배포
VITE_API_URL=https://your-backend.railway.app
